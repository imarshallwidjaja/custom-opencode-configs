[project]
name = "drawio-skill"
version = "2.1.0"
description = "Local Python environment for the drawio-skill scripts"
requires-python = ">=3.11"
dependencies = [
  "pillow>=10",
  "python-pptx>=1.0",
  "pyyaml>=6",
]

[tool.uv]
package = false
